machine:
  services:
    - docker
dependencies:
  pre:
    - docker-compose build
    - docker-compose up -d
    - docker-compose run npm install
  override:
test:
  override:
    - docker-compose run gulp
