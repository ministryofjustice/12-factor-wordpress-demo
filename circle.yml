machine:
  services:
    - docker
  php:
    version: 5.5.11
dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install zziplib-bin
    - docker-compose build
    - docker-compose up -d
  override:
test:
  override:
    - docker-compose run wordpress npm install && gulp
