machine:
  services:
    - docker
  php:
    version: 5.5.11
dependencies:
  pre:
    - docker-compose build
    - docker-compose up -d
  override:
test:
  override:
    - docker-compose run ./test.sh
